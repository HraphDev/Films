<footer id="footer" class="footer dark-background">
    <div class="container footer-top">
        <div class="row gy-4">
            <div class="col-lg-4 col-md-6 footer-about">
                <a href="{{ path('app_home') }}" class="logo d-flex align-items-center">
                    <span class="sitename">{{ site_name|default('MVS') }}</span>
                </a>
                <div class="footer-contact pt-3">
                    <p>{{ company_address|default('123 Main Street') }}</p>
                    <p>{{ company_city|default('New York, NY 10001') }}</p>
                    <p class="mt-3"><strong>Phone:</strong> <span>{{ company_phone|default('+1 234 567 8900') }}</span></p>
                    <p><strong>Email:</strong> <span>{{ company_email|default('info@example.com') }}</span></p>
                </div>
                <div class="social-links d-flex mt-4">
                    <a href="{{ social_twitter|default('#') }}"><i class="bi bi-twitter-x"></i></a>
                    <a href="{{ social_facebook|default('#') }}"><i class="bi bi-facebook"></i></a>
                    <a href="{{ social_instagram|default('#') }}"><i class="bi bi-instagram"></i></a>
                    <a href="{{ social_linkedin|default('#') }}"><i class="bi bi-linkedin"></i></a>
                </div>
            </div>

            <div class="col-lg-4 col-md-12 footer-newsletter">
                <h4>Our Newsletter</h4>
                <p>Subscribe to our newsletter for updates</p>
                {# <form action="{{ path('#') }}" method="post" class="php-email-form">
                    <div class="newsletter-form">
                        <input type="email" name="email" required>
                        <input type="submit" value="Subscribe">
                    </div>
                </form> #}
            </div>
        </div>
    </div>

    <div class="container copyright text-center mt-4">
        <p>© {{ "now"|date("Y") }} <span>Copyright</span> <strong class="px-1">{{ site_name|default('MVS') }}</strong> <span>All Rights Reserved</span></p>
    </div>
</footer>